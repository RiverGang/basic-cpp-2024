/*# include <iostream>

int main()
{
	func();
	return 0;
}

const int mum = 1000;

ClassName objName;
ClassName* p = new ClassName; // new 연산자는 힙영역에 메모리 할당, 포인터변수를 통해 스택영역에서 힙영역으로 접근
// 타입 일치 필수

char a = 'a';
int *p = &a;
=> 불가능, int type의 포인터 변수에 char type의 값을 가진 a 주소 연결 불가

*/
