/*#include <iostream>

int main() 
{
	const int num = 10; // 상수를 선언할 때는 바로 초기화를 해야함
	// int &ref = num; => const 선언을 통해 변수 num을 상수화 했는데, 다른 참조자(ref)를 통해 값을 변경하면 num의 상수화는 의미가 없다 따라서, C++에서는 이것을 허용하지 않는다

}*/